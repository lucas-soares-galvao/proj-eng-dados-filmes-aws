name: Tests (Reusable)

on:
  workflow_call:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}

        - run: |
          pip install pytest pytest-cov
          # Instala dependências de runtime de todas as apps
          find app -name 'requirements.txt' -exec pip install -r {} \;
          # Instala dependências de teste de todos os módulos de teste
          find test -name 'requirements_tests.txt' -exec pip install -r {} \;

      - run: echo "PYTHONPATH=${GITHUB_WORKSPACE}" >> $GITHUB_ENV

      - run: |
          pytest test/ \
            --cov=. \
            --cov-report=xml \
            --cov-fail-under=70

      - uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.xml