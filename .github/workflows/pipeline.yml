name: Pipeline CI/CD

on:
  push:
    branches: [develop, release, main]
  pull_request:
    branches: [develop, release, main]

env:
  ENVIRONMENT: ${{ github.ref_name == 'develop' && 'dev' || github.ref_name == 'release' && 'hom' || github.ref_name == 'main' && 'prod' }}

jobs:

  test:
    uses: ./.github/workflows/ci-test.yml

  terraform:
    needs: test
    if: github.ref_name == 'develop' || github.ref_name == 'release' || github.ref_name == 'main'
    uses: ./.github/workflows/tf-infra.yml
    with:
      environment: ${{ env.ENVIRONMENT }}
    secrets:
      aws-assume-role-arn: ${{ secrets.AWS_ROLE }}
      aws-region: ${{ secrets.AWS_REGION }}
      aws-statefile-s3-bucket: ${{ secrets.TF_BUCKET }}
      aws-lock-dynamodb-table: ${{ secrets.TF_LOCK_TABLE }}

  deploy:
    needs: terraform
    if: github.ref_name == 'develop' || github.ref_name == 'release' || github.ref_name == 'main'
    uses: ./.github/workflows/deploy.yml
    with:
      environment: ${{ env.ENVIRONMENT }}
    secrets:
      aws-assume-role-arn: ${{ secrets.AWS_ROLE }}
      aws-region: ${{ secrets.AWS_REGION }}